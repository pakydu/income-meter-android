# Gitee Go 流水线配置文件
# 正确的配置格式：pipeline 作为根节点

pipeline:
  build:
    image: gradle:7.6.3-jdk11
    commands:
      - cd /gitee/workspace/pakydu/income-meter
      - chmod +x gradlew
      - ./gradlew assembleDebug --no-daemon
    artifacts:
      - app/build/outputs/apk/debug/app-debug.apk
